<Window x:Class="BoltDownloader.Views.DownloadCompletedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource CompletedDialog_Title}" Height="260" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        Background="{DynamicResource SurfaceColor}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{DynamicResource CompletedDialog_Message}" FontWeight="Bold"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource CompletedDialog_File}" FontWeight="Bold"/>
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="txtFileName"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource CompletedDialog_Size}" FontWeight="Bold" Margin="0,8,0,0"/>
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="txtFileSize" Margin="0,8,0,0"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource CompletedDialog_Location}" FontWeight="Bold" Margin="0,8,0,0"/>
            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="txtPath" Margin="0,8,0,0" TextWrapping="Wrap"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource CompletedDialog_OpenFile}" Width="110" Height="30" Margin="0,0,8,0" Click="OpenFile_Click"/>
            <Button Content="{DynamicResource CompletedDialog_OpenFolder}" Width="110" Height="30" Margin="0,0,8,0" Click="OpenFolder_Click"/>
            <Button Content="{DynamicResource CompletedDialog_Close}" Width="100" Height="30" Click="Close_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
