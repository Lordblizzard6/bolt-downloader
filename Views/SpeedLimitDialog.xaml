<Window x:Class="BoltDownloader.Views.SpeedLimitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Límite de Velocidad" 
        Height="250" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SurfaceColor}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="Configurar límite de velocidad de descarga" 
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

        <!-- Opciones -->
        <StackPanel Grid.Row="1">
            <RadioButton x:Name="rbNoLimit" Content="Sin límite de velocidad" 
                         GroupName="SpeedLimit" IsChecked="True" 
                         Margin="0,0,0,10" Checked="SpeedOption_Changed"/>
            
            <RadioButton x:Name="rbCustomLimit" Content="Limitar velocidad a:" 
                         GroupName="SpeedLimit" Margin="0,0,0,10" 
                         Checked="SpeedOption_Changed"/>
            
            <StackPanel Orientation="Horizontal" Margin="30,0,0,10" 
                        x:Name="panelCustomSpeed" IsEnabled="False">
                <TextBox x:Name="txtSpeedLimit" Width="100" Height="25" 
                         Text="1024"/>
                <ComboBox x:Name="cmbSpeedUnit" Width="80" Height="25" 
                          Margin="10,0,0,0" SelectedIndex="0">
                    <ComboBoxItem Content="KB/s"/>
                    <ComboBoxItem Content="MB/s"/>
                </ComboBox>
            </StackPanel>

            <TextBlock Text="Nota: El límite de velocidad se aplica a todas las descargas activas." 
                       FontSize="11" Foreground="Gray" TextWrapping="Wrap" 
                       Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Botones -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Aplicar" Width="90" Height="30" 
                    Margin="0,0,10,0" Click="Apply_Click" IsDefault="True"/>
            <Button Content="Cancelar" Width="90" Height="30" 
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
