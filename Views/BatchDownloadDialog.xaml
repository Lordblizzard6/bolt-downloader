<Window x:Class="BoltDownloader.Views.BatchDownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Descarga por Lotes" 
        Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SurfaceColor}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Instrucciones -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Ingrese las URLs (una por línea):" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Puede pegar múltiples URLs desde el portapapeles" 
                       FontSize="11" Foreground="Gray"/>
        </StackPanel>

        <!-- Área de texto -->
        <TextBox Grid.Row="1" x:Name="txtUrls" 
                 AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 Margin="0,0,0,15"/>

        <!-- Botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Aceptar" Width="90" Height="30" 
                    Margin="0,0,10,0" Click="Ok_Click" IsDefault="True"/>
            <Button Content="Cancelar" Width="90" Height="30" 
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
